name: SPRING SERVER CI

on:
  push:
    branches: [ "dev", "feat/*" ]
  pull_request:
    branches: [ "dev", "main"]

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup JDK 25
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 25

      - name: Grant Gradle execution
        run: chmod +x gradlew

      - name: Copy sample files
        run: |
          cp src/main/resources/application_sample.yaml application.yaml
          cp compose_sample.yaml compose.yml

      - name: Create .env from secrets
        run: |
          echo "JWT_SECRET=${{ secrets.JWT_SECRET }}" > .env
          echo "AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY_ID }}" >> .env
          echo "AWS_SECRET_ACCESS_KEY=${{ secrets.AWS_SECRET_ACCESS_KEY }}" >> .env
          echo "S3_SERVER_ROLE=${{ secrets.S3_SERVER_ROLE }}" >> .env
          echo "S3_SERVER_REGION=${{ secrets.S3_SERVER_REGION }}" >> .env
          echo "S3_BUCKET_NAME=${{ secrets.S3_BUCKET_NAME }}" >> .env
          echo "MYSQL_USER=${{ secrets.MYSQL_USER }}" >> .env
          echo "MYSQL_PASSWORD=${{ secrets.MYSQL_PASSWORD }}" >> .env
          echo "MYSQL_DATABASE=${{ secrets.MYSQL_DATABASE }}" >> .env
          echo "MYSQL_ROOT_PASSWORD=${{ secrets.MYSQL_ROOT_PASSWORD }}" >> .env
          echo "REDIS_PASSWORD=${{ secrets.REDIS_PASSWORD }}" >> .env

      - name: Run Spring Tests
        run: ./gradlew test

      - name: Build Project
        run: ./gradlew build -x test

      - name: Docker Build Test
        run: docker build -t drone-platform:test .